## Summary

This PR fixes critical issues preventing the Feed system and Post Creation wizard from functioning correctly.

## Changes

### Firestore Indexes
- Added composite indexes for Feed filter combinations (status + environment fields + createdAt)
- Supports filtering by residence type, light direction, and experience level
- Resolves "The query requires an index" errors

### Post Creation Wizard
- Created redirect page at `/posts/create` root (was showing blank page)
- Fixed syntax errors in wizard step pages (removed stray quote characters)
- Affected pages: type, plant-info, summary, success

## Testing

- Verified Firestore index definitions are valid JSON
- Confirmed post creation wizard flow works end-to-end
- All unit tests passing

## Closes

Closes #98
